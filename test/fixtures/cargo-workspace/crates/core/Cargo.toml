[package]
name = "cargo-inspector-core"
version = "0.1.0"

edition.workspace = true

license.workspace = true

description = "Fixture library with optional JSON support"

[features]
default = []
json = [
  "dep:serde",
  "dep:serde_json"
]

[dependencies]
serde = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
