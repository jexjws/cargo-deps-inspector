[package]
name = "cargo-inspector-adapter"
version = "0.1.0"

edition.workspace = true

license.workspace = true

description = "Fixture adapter library"

[dependencies]
cargo-inspector-core = { path = "../core" }

log.workspace = true
