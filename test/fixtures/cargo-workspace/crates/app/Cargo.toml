[package]
name = "cargo-inspector-demo"
version = "0.1.0"

edition.workspace = true

license.workspace = true

description = "Fixture binary for Cargo Deps Inspector"

[dependencies]
anyhow.workspace = true

cargo-inspector-adapter = { path = "../adapter" }
cargo-inspector-core = { path = "../core", features = [ "json" ] }

[dev-dependencies]
serde_json.workspace = true
